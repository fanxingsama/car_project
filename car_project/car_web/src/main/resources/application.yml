server:
  port: 8081

spring:
  datasource:
    username: root
    password: 152906820wd
    url: jdbc:mysql://localhost:3306/car_project?useUnicode=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.entities
